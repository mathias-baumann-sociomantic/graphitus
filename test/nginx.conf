server {
    listen   *:8080;
    charset  utf-8;
    location / {
        root   /srv/graphitus;
        index  index.html;
    }
    location /graphite/ {
        proxy_pass https://srv-monitor.sociomantic.com:8080/;
    }
}
