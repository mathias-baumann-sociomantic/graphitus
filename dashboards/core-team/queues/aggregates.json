{
	"title": "Queues Aggregates",
	"width": 540,
	"defaultLineWidth": 1,
	"timeBack": "6h",
	"height": 280,
	"columns": 2,
	"refresh": true,
	"legend": false,
	"defaultParameters": "yMin=0",
	"data": [
	{
		"title": "Aggregated Connections for ${queue} Queue on ${continent}",
		"target": "alias(sum(${continent}-*.queue-${queue}.queue_node_stats.handling_connections), \"connections\")"
	},
	{
		"title": "Aggregated Processed Records/s for ${queue} Queue on ${continent}",
		"target": "alias(scale(sum(${continent}-*.queue-${queue}.queue_node_stats.records_handled),0.03333), \"records/s\")"
	},
	{
		"title": "Aggregated Bytes/s Sent for ${queue} Queue on ${continent}",
		"target": "alias(scale(sum(${continent}-*.queue-${queue}.queue_node_stats.bytes_sent),0.03333), \"b/s sent\")"
	},
	{
		"title": "Aggregated Bytes/s Received for ${queue} Queue on ${continent}",
		"target": "alias(scale(sum(${continent}-*.queue-${queue}.queue_node_stats.bytes_received),0.03333), \"b/s recv\")"
	}
	],
	"parameters": {
		"continent": {
			"All" : {"continent" : "*" },
			"EU" : {"continent" : "eu" },
			"US" : {"continent" : "us" },
			"AP" : {"continent" : "ap" },
			"USAP" : {"continent" : "usap" }
		},
		"queue": {
			"type": "dynamic",
			"query": "${continent}-*.queue-*",
			"index": 1,
			"regex": "queue-(.*)",
			"showAll": true,
			"showAllValue": "*"
		}
	}
}
